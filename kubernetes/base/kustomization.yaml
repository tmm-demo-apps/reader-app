apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - configmap.yaml
  - migrations-configmap.yaml
  - postgres.yaml
  - deployment.yaml
  - service.yaml
  - httproute.yaml
  # Note: Redis and MinIO are shared from the bookstore namespace
  # - redis://redis-service.bookstore.svc.cluster.local:6379
  # - minio-service.bookstore.svc.cluster.local:9000

images:
  - name: reader
    newName: harbor-01a.vcf.lab/bookstore/reader
    newTag: v20260827-5619cda
